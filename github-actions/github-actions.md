```mermaid
flowchart TD
  subgraph job i named joba_joba
    env[install Ubuntu]
    db[(postgress)]

    subgraph steps
      1["Checkout code actions/checkout@v3
      1. check git version
      2.
      "]
    end

  end

```
